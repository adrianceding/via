# Via TODO

本文记录 Via 接下来的功能优先级。顺序表示当前计划，开始具体开发前仍需先明确功能范围、设计原则和验收方式。

## 1. 完善多线路带宽聚合

状态：已于 2026-08-04 完成实现和自动化验收。

### 目标

- 完善现有 `adaptive + distributed`，在正常传输时把数据合理分配到多条合格线路。
- 根据预计交付时间、线路吞吐、往返时延和停滞情况动态调整分配比例。
- 快线路承担更多数据，慢线路仍能贡献有效带宽，但不能拖慢整个数据流。
- 正常传输不重复发送数据；线路故障或确认停滞时，可以临时定向补发或扩大冗余范围。
- 限制每条线路的在途数据、待发送队列和重试数量，并保证多个数据流之间的发送公平性。

### 收益

- 大文件上传和下载可以同时利用多条线路的可用带宽。
- 正常情况下不会像全量冗余模式一样成倍消耗流量。
- 单条线路变慢、丢包或断开时，已有连接仍能继续推进。
- `distributed` 从“把数据分散到多条线路”提升为可测量、可调优的带宽聚合能力。

### 完成标准

- 在受控双线路环境中，聚合吞吐明确高于最快单线路吞吐，并输出可重复的对比结果。
- 不同速率、时延、抖动和丢包组合下，数据保持字节精确、无重复、无丢失。
- 线路停滞、单向黑洞、断开和恢复时，未确认数据能够切换线路继续发送。
- 多个并发数据流共享线路时不会长期互相饿死。
- 发送窗口、重排窗口、线路在途数据和调度队列始终有固定上限。
- 进程内完整链路测试和 Linux `netns/veth/tc netem` 真实网络测试覆盖上述行为。

实现、自动化证据和维护入口见[路径质量、聚合调度与跨 Flow 公平维护指南](.local-docs/maintenance/path-quality-scheduling.md)。

## 2. 完善监控指标和结构化日志

### 目标

- 增加 Prometheus 指标接口。
- 增加可选的 JSON 结构化日志，同时保留适合终端阅读的日志格式。
- 展示每条线路的 RTT、停滞时间、吞吐、发送分配、重连和故障次数。
- 展示数据流的恢复次数、恢复耗时、重传字节、冗余字节和结束原因。
- 展示会话、数据流、队列和配置限额的当前使用量与拒绝次数。
- 让 Web Manager 展示带宽聚合效果、线路质量变化和故障恢复过程。

### 收益

- 用户可以判断 Via 为什么选择某条线路，以及多线路是否真正提高了吞吐。
- 可以区分线路故障、目标连接故障、认证失败、资源不足和本地 I/O 错误。
- 能够接入 Prometheus、Grafana 和日志采集系统进行长期观察。
- 为后续优化发送策略、TLS 和 QUIC 提供可比较的数据基线。

### 完成标准

- 指标名称、单位和含义有稳定文档。
- 指标标签不包含 Flow ID、目标地址等高基数或敏感数据。
- 日志默认不输出 PSK、密码、完整目标地址或其他凭据。
- 指标、日志和状态快照不会阻塞数据流处理。
- 历史趋势、事件队列和响应大小都有固定上限。
- 高并发和观测端不可用时，数据转发行为不受影响。

## 3. TLS 1.3

### 目标

- 使用标准库 TLS 1.3 加密客户端与服务端之间的连接。
- 客户端使用 X.509 验证服务端身份。
- 保留现有 PSK 客户端认证。
- 将 `tcp` 和 `tls` 作为明确可选的连接方式，不改变数据流恢复和发送策略。

### 收益

- 防止公网传输内容被读取或篡改。
- 防止客户端连接到伪造的服务端。
- Via 不再必须依赖外部 VPN 才能安全通过公网使用。

## 4. 多个服务端入口地址

### 目标

- 允许同一个逻辑服务端配置多个入口地址。
- 允许不同网卡选择不同入口，并支持有界的备用入口切换。
- 支持 IPv4、IPv6、不同运营商和不同公网地址的组合。
- 状态页面区分本地网卡、远端入口和连接方式。

### 收益

- 更适合双运营商、宽带加蜂窝网络和复杂路由环境。
- 单个入口不可达时，其他入口仍可建立连接。
- 服务端地址迁移和 IPv4/IPv6 过渡更平滑。

## 5. 服务端目标访问控制

### 目标

- 支持公网、私网、回环、link-local 和云元数据地址分类。
- 支持 IPv4/IPv6 CIDR 与域名允许、拒绝规则。
- 支持为不同客户端身份配置不同访问范围。
- 域名解析后检查实际拨号地址，防止 DNS rebinding 和检查结果与拨号目标不一致。

### 收益

- 防止已认证客户端访问不应开放的内网或敏感服务。
- 支持多人、多设备和不同权限范围的部署。
- 降低服务端被用作任意网络跳板的风险。

## 6. 证书和部分配置热更新

### 目标

- 更新 TLS 证书时不中断已有连接，新连接使用新证书。
- 支持安全更新目标访问规则、日志级别和观测设置。
- 新配置无效时继续使用上一份有效配置。
- 监听地址、连接方式和资源上限等结构性配置仍可要求重启。

### 收益

- 自动续期证书后不必重启服务。
- 更新访问规则和观测设置时减少连接中断。
- 降低容器和 systemd 长期部署的维护成本。

## 7. QUIC 连接方式

### 目标

- 使用成熟的标准 QUIC 库为每条本地线路建立独立连接。
- 继续由 Via 负责数据流重传、重排、线路选择和恢复。
- 避免 QUIC 与 Via 同时进行无上限重试或缓存。
- 用高丢包、高时延和多数据流测试确认实际收益。

### 收益

- 在移动网络和随机丢包环境中可能降低队头阻塞。
- 获得用户态连接管理和 TLS 1.3 能力。
- 为不同网络环境提供 TCP/TLS 之外的选择。

## 8. 新的应用入口

### 候选功能

- HTTP CONNECT。
- Linux 透明代理。
- TUN 入口。

### 目标与收益

- 复用现有可靠数据流，让更多应用无需单独配置 SOCKS5。
- 支持 Linux 网关和局域网出口场景。
- 扩大 Via 的使用范围，而不改变客户端到服务端的数据流协议。

## 9. 长期候选功能

- UDP 应用转发。
- 多服务端之间的已有数据流恢复。
- Windows 和 macOS 客户端。
- 多租户配额与审计。

这些功能会明显扩大协议、状态管理和测试范围，应根据真实用户需求分别评估，不同时推进。
